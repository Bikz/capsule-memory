{
  "name": "@capsule/cli",
  "version": "0.0.0",
  "description": "Capsule Memory CLI",
  "type": "module",
  "bin": {
    "capsule": "./dist/index.cjs"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup --config tsup.config.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@capsule/sdk-js": "workspace:*",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18.17"
  }
}
